# conda env create -f mpi4py.yaml -p mpi4py-testenv -d
name: pytorch-testenv
channels:
  - file:///glade/derecho/scratch/benkirk/conda-recipes/output
  - conda-forge
dependencies:
  - python
  - torchvision =*=*derecho*
  - numpy<2
  - mpi4py =*=*derecho*
  - conda-tree
